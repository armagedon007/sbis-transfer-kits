# 🚀 НАЧНИТЕ ЗДЕСЬ

## Что было сделано

Для вашего PHP приложения созданы все необходимые Docker конфигурационные файлы:

✅ **Dockerfile** - образ с PHP 7.4-FPM + Nginx  
✅ **docker-compose.yml** - конфигурация контейнера  
✅ **nginx.conf** - настройки веб-сервера  
✅ **default.conf** - маршрутизация запросов  
✅ **Документация** - подробные инструкции  
✅ **Тестовые скрипты** - автоматическая проверка  

---

## ⚡ Быстрый запуск (3 команды)

### 1. Запустите Docker контейнер
```bash
docker-compose up -d --build
```

### 2. Откройте приложение в браузере
```
http://localhost:8000/
```

### 3. Проверьте логи (опционально)
```bash
docker-compose logs -f
```

**Готово!** Ваше приложение работает! 🎉

---

## 📋 Что работает

| URL | Описание |
|-----|----------|
| http://localhost:8000/ | Frontend (index.html) |
| http://localhost:8000/backend/api/warehouses.php | API: Склады |
| http://localhost:8000/backend/api/kits.php | API: Комплекты |
| http://localhost:8000/backend/api/products.php | API: Продукты |
| http://localhost:8000/backend/api/documents.php | API: Документы |

---

## 🔧 Управление контейнером

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Остановка и удаление
docker-compose down

# Пересборка
docker-compose up -d --build --force-recreate
```

---

## 🧪 Автоматическое тестирование

### Linux/Mac:
```bash
chmod +x test-docker.sh
./test-docker.sh
```

### Windows:
```cmd
test-docker.bat
```

Скрипт проверит:
- ✅ Установку Docker
- ✅ Сборку образа
- ✅ Запуск контейнера
- ✅ Доступность frontend
- ✅ Работу PHP и Nginx
- ✅ PHP расширения

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| **QUICK_START.md** | Быстрый старт и основные команды |
| **DOCKER_README.md** | Полная документация по Docker |
| **API_EXAMPLES.md** | Примеры использования API |
| **CHECKLIST.md** | Чеклист проверки настройки |
| **DOCKER_FILES_SUMMARY.md** | Описание всех созданных файлов |

---

## ⚙️ Технические детали

### PHP
- Версия: 7.4-FPM
- Расширения: curl, json, dom, mbstring

### Nginx
- Веб-сервер на порту 8000
- Маршрутизация:
  - `/` → frontend/index.html
  - `/backend/api/*` → PHP-FPM
  - Статические файлы (CSS, JS)

### Docker
- Volumes для автоматического обновления кода
- CORS настроен для API
- Логирование Nginx и PHP

---

## 🐛 Возможные проблемы

### Порт 8000 занят
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:8000"  # Используйте другой порт
```

### 502 Bad Gateway
Перезапустите контейнер:
```bash
docker-compose restart
```

### Ошибки прав доступа
```bash
docker exec php-app chown -R www-data:www-data /var/www/html
```

### Подробнее
См. раздел Troubleshooting в `DOCKER_README.md`

---

## 📝 Настройка приложения

Не забудьте настроить `backend/config.php` с вашими учетными данными для СБИС API.

---

## 🎯 Следующие шаги

1. ✅ Запустите: `docker-compose up -d --build`
2. ✅ Откройте: http://localhost:8000/
3. ✅ Настройте: `backend/config.php`
4. ✅ Тестируйте: API endpoints

---

## 💡 Полезные команды

```bash
# Вход в контейнер
docker exec -it php-app bash

# Проверка PHP
docker exec php-app php -v
docker exec php-app php -m

# Проверка Nginx
docker exec php-app nginx -t

# Логи Nginx
docker exec php-app tail -f /var/log/nginx/access.log
docker exec php-app tail -f /var/log/nginx/error.log

# Перезагрузка Nginx
docker exec php-app nginx -s reload
```

---

## ✨ Готово!

Все настроено и готово к использованию.

**Запустите:** `docker-compose up -d --build`

**Откройте:** http://localhost:8000/

Удачи! 🚀
