# ✅ Чеклист настройки Docker

## Созданные файлы

- [x] **Dockerfile** - образ с PHP 7.4-FPM и Nginx
- [x] **docker-compose.yml** - конфигурация контейнера
- [x] **nginx.conf** - основная конфигурация Nginx
- [x] **default.conf** - конфигурация виртуального хоста
- [x] **.dockerignore** - исключения для Docker образа
- [x] **php.ini.example** - пример настроек PHP
- [x] **test-docker.sh** - скрипт тестирования (Linux/Mac)
- [x] **test-docker.bat** - скрипт тестирования (Windows)
- [x] **DOCKER_README.md** - полная документация
- [x] **QUICK_START.md** - быстрый старт
- [x] **API_EXAMPLES.md** - примеры использования API

## Требования выполнены

### PHP
- [x] PHP 7.4+
- [x] Расширение curl
- [x] Расширение json
- [x] Расширение dom
- [x] Расширение mbstring

### Nginx
- [x] Nginx как веб-сервер
- [x] Корневой URL "/" открывает frontend/index.html
- [x] Статические файлы (CSS, JS) доступны из корня
- [x] API запросы /backend/api/* проксируются на PHP-FPM
- [x] PHP-FPM обрабатывает PHP файлы

### Docker
- [x] Порт 8000
- [x] Volumes для автоматического обновления кода
- [x] Оптимизированный .dockerignore

### Дополнительно
- [x] CORS настроен для API
- [x] Логирование Nginx и PHP
- [x] Скрипты для тестирования
- [x] Подробная документация

## Быстрая проверка

### 1. Запуск
```bash
docker-compose up -d --build
```

### 2. Проверка статуса
```bash
docker-compose ps
```

Должно быть: `State: Up`

### 3. Проверка frontend
Откройте в браузере: http://localhost:8000/

Должна открыться страница "Внутреннее перемещение комплектов"

### 4. Проверка API
```bash
curl http://localhost:8000/backend/api/warehouses.php
```

Должен вернуться JSON с данными

### 5. Проверка логов
```bash
docker-compose logs
```

Не должно быть критических ошибок

## Структура маршрутов

| URL | Обработчик | Описание |
|-----|-----------|----------|
| `/` | Nginx → frontend/index.html | Главная страница |
| `/style.css` | Nginx → frontend/style.css | CSS файл |
| `/app.js` | Nginx → frontend/app.js | JavaScript файл |
| `/frontend/*` | Nginx → frontend/* | Статические файлы |
| `/backend/api/*.php` | PHP-FPM | API endpoints |
| `/backend/*.php` | PHP-FPM | Другие PHP файлы |

## Тестирование

### Автоматическое тестирование

**Linux/Mac:**
```bash
chmod +x test-docker.sh
./test-docker.sh
```

**Windows:**
```cmd
test-docker.bat
```

### Ручное тестирование

1. **Frontend доступен:**
   - http://localhost:8000/ → должна открыться страница

2. **Статические файлы загружаются:**
   - http://localhost:8000/style.css → CSS код
   - http://localhost:8000/app.js → JavaScript код

3. **API работает:**
   - http://localhost:8000/backend/api/warehouses.php → JSON ответ
   - http://localhost:8000/backend/api/kits.php → JSON ответ
   - http://localhost:8000/backend/api/products.php → JSON ответ

4. **PHP работает:**
   ```bash
   docker exec php-app php -v
   docker exec php-app php -m | grep -E "(curl|json|dom|mbstring)"
   ```

5. **Nginx работает:**
   ```bash
   docker exec php-app nginx -t
   ```

## Возможные проблемы

### Порт 8000 занят
**Решение:** Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:8000"  # Используйте 8080 вместо 8000
```

### Ошибки прав доступа
**Решение:**
```bash
docker exec php-app chown -R www-data:www-data /var/www/html
docker exec php-app chmod -R 755 /var/www/html
```

### 502 Bad Gateway
**Причина:** PHP-FPM не запущен

**Решение:**
```bash
docker-compose restart
docker-compose logs
```

### Статические файлы не загружаются
**Причина:** Неправильные пути в HTML

**Решение:** Проверьте, что в `frontend/index.html` используются относительные пути:
```html
<link rel="stylesheet" href="style.css">
<script src="app.js"></script>
```

## Следующие шаги

1. ✅ Запустите контейнер: `docker-compose up -d --build`
2. ✅ Проверьте frontend: http://localhost:8000/
3. ✅ Проверьте API: http://localhost:8000/backend/api/warehouses.php
4. ✅ Просмотрите логи: `docker-compose logs -f`
5. ✅ При необходимости настройте `backend/config.php`

## Полезные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Вход в контейнер
docker exec -it php-app bash

# Остановка и удаление
docker-compose down

# Пересборка
docker-compose up -d --build --force-recreate
```

---

**Готово! 🎉**

Все файлы созданы и готовы к использованию.
